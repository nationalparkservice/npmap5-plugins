/**
 * Converts a string to an integer.
 * @param str - The string to convert to an integer.
 * @returns The integer value of the string.
 */
export default function convertToInt(str: string): number {
    return parseInt(str, 10);
}